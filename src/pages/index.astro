---
import Layout from "../layouts/Layout.astro";
import { TEMPLATES } from "../lib/templates";

<<<<<<< Updated upstream
// 7 floating template card positions matching the reference design:
// 1. top-left card
// 2. upper-left/mid card
// 3. top-right card
// 4. right-middle card
// 5. bottom-left card
// 6. bottom-right card
// 7. lower-right card
const heroSlots = [
    // 1. top-left card
    { ...TEMPLATES[0], pos: "left-[3%] top-[8%] w-40 xl:w-46 -rotate-6" },
    // 2. upper-left/mid card
    { ...TEMPLATES[1], pos: "left-[2%] top-[38%] w-38 xl:w-44 rotate-6" },
    // 3. bottom-left card
    { ...TEMPLATES[4], pos: "left-[3%] bottom-[8%] w-40 xl:w-46 -rotate-3" },
    // 4. top-right card
    { ...TEMPLATES[2], pos: "right-[3%] top-[8%] w-40 xl:w-46 rotate-6" },
    // 5. right-middle card
    { ...TEMPLATES[3], pos: "right-[2%] top-[38%] w-38 xl:w-44 -rotate-6" },
    // 6. bottom-right card
    { ...TEMPLATES[5], pos: "right-[3%] bottom-[8%] w-40 xl:w-46 rotate-3" },
    // 7. lower-right card
    { ...TEMPLATES[6], pos: "right-[16%] bottom-[4%] w-36 xl:w-40 -rotate-6" },
=======
// A dense "meme wall" scatter behind the hero copy. Cycles through the full
// template pool (wrapping around) across many more slots than templates so
// the background reads as a packed collage rather than a handful of floating
// cards.
const heroSlotLayout = [
    { pos: "left-[2%] top-[6%] w-36 -rotate-6" },
    { pos: "left-[1%] top-[38%] w-28 rotate-3" },
    { pos: "left-[3%] top-[66%] w-40 rotate-6" },
    { pos: "left-[16%] top-[2%] w-28 rotate-3" },
    { pos: "left-[14%] top-[80%] w-32 -rotate-3" },
    { pos: "left-[26%] top-[30%] w-24 -rotate-6" },
    { pos: "right-[2%] top-[8%] w-40 rotate-3" },
    { pos: "right-[1%] top-[40%] w-28 -rotate-3" },
    { pos: "right-[3%] top-[68%] w-40 -rotate-6" },
    { pos: "right-[17%] top-[4%] w-28 -rotate-3" },
    { pos: "right-[15%] top-[82%] w-32 rotate-3" },
    { pos: "right-[27%] top-[32%] w-24 rotate-6" },
];

const heroSlots = heroSlotLayout.map((slot, i) => ({
    ...TEMPLATES[i % TEMPLATES.length],
    pos: slot.pos,
}));

const features = [
    {
        title: "Edit.",
        body: "Drop in any template or upload your own image. The canvas renders at full resolution, right in your browser.",
    },
    {
        title: "Customize.",
        body: "Drag captions into place, tune size and color, and get classic Impact-style outlined text — auto-sized to fit.",
    },
    {
        title: "Download.",
        body: "Export a PNG in one click. No account, no watermark, no upload to a server — it never leaves your device.",
    },
>>>>>>> Stashed changes
];
---

<Layout
    title="MemeMaker — Free online meme generator, no sign-up."
    showHeader={false}
    showFooter={false}
>
    <main
        class="relative flex min-h-screen w-full flex-col justify-between overflow-hidden px-4 pb-8 pt-[28px] sm:px-6"
    >
        <!-- Full-viewport pastel mesh gradient backdrop -->
        <div
            class="mesh-gradient pointer-events-none absolute inset-0 -z-10 h-full w-full"
            aria-hidden="true"
        >
        </div>

        <!-- 1. HEADER: Centered floating glassmorphism navigation pill -->
        <header
            class="relative z-30 mx-auto flex h-[68px] w-full max-w-[680px] items-center justify-between rounded-full border border-white/80 bg-white/70 px-5 shadow-[0_8px_30px_rgb(0,0,0,0.06)] backdrop-blur-xl transition-all sm:px-6"
        >
            <!-- Left: Logo & text -->
            <a
                href="/"
                class="flex items-center gap-2.5 text-ink outline-none transition-opacity hover:opacity-85 focus-visible:ring-2 focus-visible:ring-link focus-visible:ring-offset-2"
            >
                <span
                    class="flex h-8 w-8 items-center justify-center rounded-xl bg-ink text-white shadow-sm"
                >
                    <svg
                        width="18"
                        height="18"
                        viewBox="0 0 16 16"
                        fill="none"
                        aria-hidden="true"
                    >
                        <path d="M8 1L15 14H1L8 1Z" fill="currentColor"></path>
                    </svg>
                </span>
                <span class="text-[16px] font-bold tracking-tight text-ink">
                    MemeMaker
                </span>
            </a>

            <!-- Center: Navigation links -->
            <nav
                class="hidden items-center gap-7 sm:flex"
                aria-label="Primary Navigation"
            >
                <a
                    href="/edit"
                    class="text-sm font-medium text-body/90 transition-colors hover:text-ink"
                >
                    Templates
                </a>
                <a
                    href="/edit"
                    class="text-sm font-medium text-body/90 transition-colors hover:text-ink"
                >
                    Editor
                </a>
            </nav>

            <!-- Right: Create Meme button -->
            <a
                href="/edit"
                class="inline-flex h-11 items-center justify-center rounded-full bg-ink px-5 text-sm font-semibold text-white shadow-sm outline-none transition-all hover:bg-black/85 hover:shadow focus-visible:ring-2 focus-visible:ring-link focus-visible:ring-offset-2"
            >
                Create Meme
            </a>
        </header>

        <!-- 8. FLOATING IMAGES: 7 scattered template cards around the hero (desktop) -->
        <div
            class="pointer-events-none absolute inset-0 hidden lg:block"
            aria-hidden="false"
        >
            {
                heroSlots.map((slot) => (
                    <a
                        href={`/edit?template=${slot.id}`}
                        class:list={[
                            "hero-slot group pointer-events-auto absolute block transition-transform duration-300 hover:scale-105 hover:z-20",
                            slot.pos,
                        ]}
                    >
                        <span class="relative block overflow-hidden rounded-2xl border border-white/80 bg-white/40 p-1 shadow-lg shadow-black/8 backdrop-blur-sm">
                            <img
                                src={slot.src}
                                alt={slot.name}
                                width={slot.width}
                                height={slot.height}
                                class="aspect-4/3 w-full rounded-xl object-cover opacity-90 transition-opacity group-hover:opacity-100"
                                loading="eager"
                            />
                            <span class="absolute inset-0 flex items-center justify-center rounded-xl bg-ink/40 opacity-0 transition-opacity group-hover:opacity-100">
                                <span class="rounded-full bg-white px-3 py-1.5 text-xs font-semibold text-ink shadow-md">
                                    Use Template
                                </span>
                            </span>
                        </span>
                    </a>
                ))
            }
        </div>

        <!-- Mobile template preview strip (mobile only) -->
        <div
            class="-mx-4 flex gap-3 overflow-x-auto px-4 pb-2 pt-4 lg:hidden"
            aria-label="Meme templates"
        >
            {
                TEMPLATES.slice(0, 7).map((t) => (
                    <a
                        href={`/edit?template=${t.id}`}
                        class="block shrink-0 overflow-hidden rounded-xl border border-white/70 bg-white/40 p-0.5 shadow-md backdrop-blur-sm"
                    >
                        <img
                            src={t.src}
                            alt={t.name}
                            width={t.width}
                            height={t.height}
                            class="aspect-4/3 w-28 rounded-lg object-cover"
                        />
                    </a>
                ))
            }
        </div>

        <!-- 2. HERO POSITION: Centered hero content below header -->
        <div
            class="relative z-10 mx-auto my-auto flex w-full max-w-[640px] flex-col items-center pt-[50px] text-center sm:pt-[65px]"
        >
            <!-- 3. BADGE: ~70px below header -->
            <span
                class="inline-flex items-center rounded-full border border-black/5 bg-white/75 px-4 py-1.5 font-mono text-xs font-medium text-body shadow-sm backdrop-blur-md"
            >
                free · no sign-up · no watermark
            </span>

            <!-- 4. TITLE: Large bold/black heading, ~48-52px on desktop -->
            <h1
                class="mt-4 text-[40px] font-extrabold leading-[1.1] tracking-[-0.035em] text-ink sm:text-[48px] md:text-[52px]"
            >
                Make a meme in seconds.
            </h1>

            <!-- 5. SUBTITLE: Center aligned, max-width ~600px, line-height ~1.5 -->
            <p
                class="mt-3.5 max-w-[600px] text-base leading-[1.5] text-body sm:text-[17px]"
            >
                Pick a template or upload any image, add your captions, and
                download the PNG. Entirely in your browser — nothing is ever
                uploaded to a server.
            </p>

            <!-- 6. BUTTONS: Horizontal & centered -->
            <div class="mt-7 flex flex-wrap items-center justify-center gap-3.5">
                <!-- Start Creating (black background, magic wand/sparkle icon) -->
                <a
                    href="/edit"
                    class="inline-flex h-12 items-center justify-center gap-2.5 rounded-full bg-ink px-7 text-sm font-semibold text-white shadow-md shadow-black/10 outline-none transition-all hover:scale-[1.02] hover:bg-black/85 active:scale-[0.98] focus-visible:ring-2 focus-visible:ring-link focus-visible:ring-offset-2 sm:text-base"
                >
                    <svg
                        width="18"
                        height="18"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        aria-hidden="true"
                    >
                        <path
                            d="m12 3-1.9 5.8a2 2 0 0 1-1.3 1.3L3 12l5.8 1.9a2 2 0 0 1 1.3 1.3L12 21l1.9-5.8a2 2 0 0 1 1.3-1.3L21 12l-5.8-1.9a2 2 0 0 1-1.3-1.3L12 3Z"
                        ></path>
                        <path d="M5 3v4"></path>
                        <path d="M19 17v4"></path>
                        <path d="M3 5h4"></path>
                        <path d="M17 19h4"></path>
                    </svg>
                    <span>Start Creating</span>
                </a>

                <!-- Browse Templates (white background, grid icon) -->
                <a
                    href="/edit"
                    class="inline-flex h-12 items-center justify-center gap-2.5 rounded-full border border-black/10 bg-white/90 px-7 text-sm font-semibold text-ink shadow-sm backdrop-blur-sm outline-none transition-all hover:scale-[1.02] hover:border-black/20 hover:bg-white active:scale-[0.98] focus-visible:ring-2 focus-visible:ring-link sm:text-base"
                >
                    <svg
                        width="18"
                        height="18"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        aria-hidden="true"
                    >
                        <rect width="7" height="7" x="3" y="3" rx="1"></rect>
                        <rect width="7" height="7" x="14" y="3" rx="1"></rect>
                        <rect width="7" height="14" x="14" y="14" rx="1"></rect>
                        <rect width="7" height="7" x="3" y="14" rx="1"></rect>
                    </svg>
                    <span>Browse Templates</span>
                </a>
            </div>

            <!-- 7. UPLOAD BOX: ~570px wide, ~175px tall, dashed border, translucent glass -->
            <div
                data-dropzone
                tabindex="0"
                role="button"
                aria-label="Upload an image to start editing"
                class="group mt-8 flex h-[175px] w-full max-w-[570px] cursor-pointer flex-col items-center justify-center gap-2 rounded-2xl border-2 border-dashed border-black/15 bg-white/55 px-6 py-6 text-center shadow-[0_8px_30px_rgb(0,0,0,0.04)] backdrop-blur-md outline-none transition-all hover:border-ink hover:bg-white/75 focus-visible:ring-2 focus-visible:ring-link sm:mt-9"
            >
                <svg
                    width="28"
                    height="28"
                    viewBox="0 0 24 24"
                    fill="none"
                    class="text-mute transition-colors group-hover:text-ink"
                    aria-hidden="true"
                >
                    <path
                        d="M12 16V4M12 4L7 9M12 4L17 9M5 16v2a2 2 0 002 2h10a2 2 0 002-2v-2"
                        stroke="currentColor"
                        stroke-width="1.8"
                        stroke-linecap="round"
                        stroke-linejoin="round"></path>
                </svg>
                <p class="text-[15px] font-semibold text-ink">
                    Drop an image, or click to upload
                </p>
                <p class="font-mono text-xs text-mute">
                    PNG, JPG, WEBP – processed locally
                </p>
                <input
                    data-dropzone-input
                    type="file"
                    accept="image/*"
                    class="hidden"
                />
            </div>
        </div>

        <!-- Spacer for balanced vertical layout -->
        <div class="h-2 sm:h-4" aria-hidden="true"></div>
    </main>

    <script src="../scripts/landing.ts"></script>
</Layout>
